//! Grimoire overlay — stub; the real overlay (mirroring the native
//! `HelpWindow` content) replaces this file.

pub fn toggle() {}

pub fn is_open() -> bool {
    false
}

/// Close if open; true when something was closed.
pub fn close() -> bool {
    false
}
