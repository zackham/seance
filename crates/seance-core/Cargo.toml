[package]
name = "seance-core"
version.workspace = true
edition.workspace = true
description = "Sans-io seance wire protocol: control + GUI wire types, SCG3 grid codec, key→PTY-byte encoding. Compiles for native and wasm32."
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
base64 = { workspace = true }
